# ── Local deploy state / resource-tracking files (regenerated by the scripts) ──
# These capture live AWS resource IDs, ARNs, and endpoints from YOUR run.
# They are environment-specific and can leak account details — never commit.
*_resources.env
part4_training.env
part4_registry.env
crossregion_resources.env
failover_resources.env

# ── Part 1 / Part 4 generated data & model artifacts ──
model_evaluation_results.csv
model_selection_strategy.json
financial_qa_dataset.csv
*.tar.gz
model/
model_repack/

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# ── Packaging scratch (zips the deploy scripts build) ──
/tmp/
*.zip

# ── OS / editor noise ──
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# ── Anything obviously secret (defensive) ──
*.pem
*.key
credentials
.env

# ── Private / not-for-publication docs ──
# BLOG_OUTLINE.md is LinkedIn material — keep it local, never publish to GitHub.
BLOG_OUTLINE.md
LINKEDIN_POST.md
